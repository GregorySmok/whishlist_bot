# 1. Берем за основу готовый легкий Python
FROM python:3.10.2-slim

# 2. Указываем папку внутри "коробки", где будет лежать бот
WORKDIR /app

# 3. Копируем файл с библиотеками внутрь
COPY requirements.txt .

# 4. Устанавливаем библиотеки внутри "коробки"
RUN pip install --no-cache-dir -r requirements.txt

# 5. Копируем весь остальной код проекта внутрь
COPY . .

# 6. Команда, которая запустит бота
CMD ["python", "main.py"]